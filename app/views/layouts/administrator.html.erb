<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%=h yield(:title) %> - Kalité</title>
  <meta name="description" content="">
  <meta name="author" content="">
  <%= javascript_include_tag :defaults %>
  
  <%= csrf_meta_tag %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->      
  
  <!-- Le styles -->
  <%= stylesheet_link_tag "application" %>
	<%= stylesheet_link_tag "bootstrap.min"%>
  <style type="text/css">
    body {
      padding-top: 60px;
    }
  </style>

  <!-- Le fav and touch icons -->
</head>

<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/">Área de administración</a>
        <div class="nav-collapse">
          <ul class="nav">
            <% if yield(:title) == 'Revisiones Técnicas Formales' %>
              <li class="active">
            <% else %>
              <li>
            <% end %>
              <%= link_to "Revisiones Técnicas Formales", administrator_home_show_formal_technical_reviews_path %>
              </li>
            <% if yield(:title) == 'Catálogos' %>
              <li class="active">
            <% else %>
              <li>
            <% end %>
             <%= link_to "Catálogos", administrator_home_display_options_path %>
              </li>
            <% if yield(:title) == 'Ayuda' %>
              <li class="active">
            <% else %>
              <li>
            <% end %>
             <%= link_to "Ayuda", administrator_home_help_options_path %>
            </li>
          </ul>
         <p class="navbar-text pull-right">
            Usted está en el sistema como: <b><%= current_administrator.first_name %> <%= current_administrator.last_name %></b>
            <%= link_to "Salir", destroy_administrator_session_path, :method => :delete %>
         </p>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>

  <div class="container">
    <% if yield(:title) == 'Catálogos' %>
    <div class="row-fluid">
      <div class="span2">
        <%= render "shared/administrator_menu" %>
      </div>
      <div class="span9">
          <%= render "shared/flash_messages" %>
          <%= yield %>
      </div>
    </div>
      
    <% else %>
      <%= render "shared/flash_messages" %>				       
      <%= yield %>
    <% end %>

    
    
    
  </div><!--/.fluid-container-->
  <footer>
      <hr/>
        <p>
          &nbsp; Centro de Servicios Tecnológicos<br/>
          &nbsp;&copy; <%= Time.now.year %> Laboratorio Nacional de Informática Avanzada. Todos los derechos reservados
        </p>
     </footer>
</body>

</html>
